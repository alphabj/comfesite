// ©2012 Comfe Designs LLC.

$(function(){

	//set a new instance of dotImageCycle class and start it up 	
	homepageDots = new dotImageCycle();
	homepageDots.startupSeq($('img.carousel :first'), $('.slideshow li:first'));
	//homepageDots.headlineStart($('.headine h1:first'));

	$('ul.slideshow li').click(function(){  //add .home class for just homepage. 
		var current = $(this).index();
		if(homepageDots.isOperating == false) {
			homepageDots.isOperating = true;
			//clearTimeout(homepageDots.timer);
			if(homepageDots.intvlSet == true) { 
				//clearInterval(homepageDots.autoChange.timer); 
				//homepageDots.intvlSet = false; 
			}
			if( homepageDots.previous != current ){	//ignore same clicks	
				homepageDots.changeDots($('.slideshow li.selected'), $('.slideshow li').eq(current));
				homepageDots.curtainCall($('img.nowShowing'), $('img.carousel').eq(current));
				//headlineSwitch($('.headline h1.nowShowing'), $('.headline h1').eq(position) );
				homepageDots.previous = current;
			}
			homepageDots.isOperating = false;
		}
		//setTimeout(homepageDots.autoChange(), 4000);
	});
	
});